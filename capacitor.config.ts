import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'xhr-post',
  webDir: 'www',
//   server: {
//     allowNavigation: ['*.typicode.com']
// }
};

export default config;
